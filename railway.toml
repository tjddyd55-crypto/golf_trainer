# Railway 프로젝트 설정 파일
# 이 파일은 Railway CLI를 사용할 때 참조됩니다

[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn --chdir . services.super_admin.app:app --bind 0.0.0.0:$PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

[env]
PYTHONPATH = "."
